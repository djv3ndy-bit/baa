{
  "private": true,
  "type": "module",
  "engines": { "node": "22.x" },
  "scripts": { "test": "node launch-check.js && node --test tests/*.test.js tests/*.test.mjs" },
  "dependencies": { "stripe": "22.4.0" }
}
