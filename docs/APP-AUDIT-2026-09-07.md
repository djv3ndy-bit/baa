# BaristaMatch app audit — September 7, 2026

Release candidate: **1.0.4**, iOS and Android. Website parity baseline: main `a54e180` (private café pricing and website account repairs). This audit records source repairs and verified checks; a source test pass is not a store approval or physical-device result.

## Repairs included

- **Authentication:** Saved account role is authoritative. New OAuth members choose a role and complete signup explicitly; email verification does not write profiles without a session. Duplicate callback exchanges share one request. Login, cancellation, verification resend and password recovery have recoverable errors. Verified café sessions initialize the existing complimentary hiring access before returning a ready workspace. Concurrent initialization is shared, failures can retry, and the request is bound to the reviewed account token.
- **Profiles:** Required-field completion includes a photo and valid private birth date. Gender stays optional and private. Both roles can explicitly control discovery visibility. Save confirms the server's returned row, preserves saved opt-outs, saves private data before public eligibility, protects uploads/drafts during navigation and retries, and restores the last saved profile on Cancel.
- **Marketplace:** Exact Florida city/ZIP search matches the website. Pagination does not discard candidates after the first hundred results. Applying to a particular job is separate from expressing interest in a café. Incoming/sent interests and application history remain reachable independently of active jobs. Job edits retain pay ranges, custom schedules and applicants; cafés can pause and reopen posts.
- **Messages:** Conversations, drafts and in-flight requests are tied to their account and recipient. Stable client message IDs make uncertain retries idempotent. Unread notifications use the existing application receipts and new discovery receipts, with matching read acknowledgements. A paused job no longer breaks a previously matched conversation. The metadata endpoint checks membership and blocks before returning café identity.
- **Home and notifications:** Dashboard counts include job applications, sent interests, both match types and unread conversations. Taps lead to the relevant lists. Push routes are validated and preserve conversation kind/job ID. Notification permission is requested from a user-triggered Settings control; denied permission is reported accurately.
- **Settings and account lifecycle:** Café plans stay hidden until the saved café role is verified, and hide on sign-out/account change. Profile and billing errors offer retry instead of false active status. Password changes use the reviewed account's captured token. Deletion confirmations retain their original account ID. A serialized auth-storage adapter prevents cleanup from erasing a different account signing in concurrently. API and Supabase requests have deadlines.
- **Traffic and dependencies:** Eight live row-security identity checks now run once per statement, and discovery messages have a sender index. Permissions and billing rules are unchanged. Decoder/PostCSS/UUID vulnerabilities received compatible fixes; Expo/React Native majors were retained. The unpatched image parser is mitigated by rejecting affected build formats in both Metro processes. See [dependency evidence](../mobile/DEPENDENCY_SECURITY.md).

## Verification evidence

- 386 repository tests passed, including actual-screen mocked interactions and real installed auth-client/storage concurrency tests.
- 302 Yoga layout tests passed across narrow/wide screens and text scaling. These use synthetic font metrics, not device screenshots.
- 39 isolated PostgreSQL tests passed. Cross-account/anonymous access and unchanged policy semantics are tested, with query-plan checks for the performance repair. Seven regressions execute the actual café access functions and policies: a new café starts without hiring access, and its verified initialization creates the required access row.
- 26 Android archive-checker regressions are included in the repository total; the verifier distinguishes ELF alignment, AAB packaging requests and actual APK offsets.
- TypeScript, release/authentication configuration checks and `launch-check.js` passed.
- Expo Doctor: 17/17 checks passed after a clean lockfile installation.
- iOS and Android Hermes bundles exported successfully during dependency review using synthetic configuration; final signed cloud build evidence is recorded separately below.
- Live migration history confirms `20260907061824_optimize_account_policy_auth_checks`; the repository uses that same version.
- Live Supabase security advisors: no findings. After the performance migration, no performance warnings or missing foreign-key indexes remain. Informational unused-index and auth connection-allocation notices remain; early-stage indexes were retained.
- Cloud build images defaulted to Node 20 despite the locked Supabase client requiring Node 22+. All build profiles now pin Node 22.23.2, matching the supported CI major; release checks reject unsupported overrides. [Node release archive](https://nodejs.org/en/download/archive/v22).
- Expo history before this release: the latest prior iOS production build was 1.0.3/build 6 at `5f90c1d`; latest Android was a 1.0.3 preview APK/code 2. Those artifacts do not contain these repairs.

## Release evidence and remaining acceptance

Verified on September 7, 2026 at 06:57 UTC:

- [Repair PR #49](https://github.com/djv3ndy-bit/baa/pull/49) merged as `5182a841a71c5c8230953bc772a3347f5ae6e1a2`. All seven final PR workflows passed. The iOS candidate was built from `0c26daf57400c892c92bb884f7a24af08883a748`; its complete source tree was verified identical to the merge.
- Website production deployment `dpl_BB2wfHPeu9LzuVFuSL331dajuZjE` is READY on `baristajobmatch.com` and `www.baristajobmatch.com` at that merge. The new conversation endpoint returns 401 without authentication; the message endpoint rejects GET with 405. The one-hour runtime scan reported no errors. No capacity/load claim is made from these checks.
- [iOS build 1.0.4 (7)](https://expo.dev/accounts/tovendy/projects/baristamatch/builds/3ad12f4b-2db6-4115-b31d-57a64134b9f6) finished. The IPA SHA256 is `433416263d06094df79029168e008a828457ccb030887304af55a8801802c34c`. Actual metadata confirms bundle `com.baristajobmatch.app`, iPhoneOS SDK 26.0, minimum iOS 15.1 and the `baristamatch` callback scheme. Thirteen privacy manifests are present; their inventory does not certify the store privacy answers.
- Read-only macOS signing verification passed for the app and embedded frameworks (`codesign --verify --deep --strict`). The signed team is `B62266JK98`, push environment is `production`, and debugging permission is false. The matching App Store provisioning profile and allowed certificate expire August 27, 2027. Initial sandbox certificate-access failures were resolved by allowing the same read-only signing checks to access macOS signing services; the artifact was not altered.
- [iOS submission](https://expo.dev/accounts/tovendy/projects/baristamatch/submissions/aabfe3be-60b9-4ca2-a0cc-63bf0ce73d63) finished. App Store Connect independently reports the exact 1.0.4/build 7 as VALID and IN_BETA_TESTING internally. External testing is READY_FOR_BETA_SUBMISSION; no live, in-review or pending-public-release version was returned. No external beta review or public release was requested.
- The optional automated TestFlight changelog field was rejected because it requires an Expo Enterprise plan. Upload succeeded without it. The manual release workflow now keeps the full test checklist in its document step and omits the paid submission field; all 380 release guards and EAS workflow schema validation pass.

Android 1.0.4 has no signed production artifact yet: the real Firebase configuration and FCM credentials are missing. Public app-store availability is not established by an internal TestFlight build.

The current host has command-line Apple tools but no installed Xcode simulator or Android device toolchain. Browser automation was blocked by the browser security policy; no alternate browser route was used. No physical iPhone/Android walkthrough, screenshots, real account deletion, live messages, payments or push receipt was claimed.

Current external configuration gaps verified through EAS metadata: no Android FCM V1 service-account key or Play submission service-account key is configured; the production environment has no Firebase client configuration file. Android release configuration now validates the real `GOOGLE_SERVICES_JSON` file and stops production Android builds when it is missing or mismatched. The two required public Supabase variables are configured; a value check confirmed the URL matches the website project and the key is a publishable client key, not a privileged service key. Raw values were not printed or saved. These checks do not verify unknown manual Play Console uploads or Google developer organization identity. The saved Apple login identified team B62266JK98 as an Individual account under Tovendy Pierre, so LLC seller identity is not established. The missing APNs key was created and assigned through EAS using the existing Apple login; an independent metadata read verified assignment to the app and matching team. No existing credentials were revoked. Phone delivery still needs a signed-device test.

Remaining acceptance work:

1. Complete Android Firebase/FCM and Play submission setup, build the signed AAB, and verify its SDK, permissions, generated APK packaging and native-library 16 KB alignment. iOS signing/configuration checks above are complete; actual push receipt remains a device check.
2. Run disposable café/barista accounts through signup/email/OAuth/reset, profiles/media/visibility, exact-area discovery, job applications and interests, match/chat/unread/push, blocking/reporting, settings, logout and account deletion on signed devices. Include airplane mode, background/foreground, cold links and large text.
3. Confirm the current Apple Developer/Google Play organization identity, agreements and app records. Historical seller-verification holds are not treated as proof of current console status.
4. Complete accurate App Privacy/Data Safety, content/age/ads questionnaires and private reviewer access. Existing public privacy, terms, support and account-deletion pages are already supplied in the submission records.
5. Supply genuine screenshots from the final signed app and final store artwork; generated marketing artwork must not be presented as a device screenshot.
6. Confirm app review/processing and store publication. Existing paid-subscription management is retained; new native purchases remain unavailable and the website billing rollout is still paused.

Apple's current upload baseline is iOS SDK 26+, and Google Play's current new-app/update baseline is Android API 36. An ELF alignment check alone does not prove Android runtime compatibility on a 16 KB device. Sources: [Apple submissions](https://developer.apple.com/app-store/submitting/), [Google Play target SDK](https://developer.android.com/google/play/requirements/target-sdk), [Android page sizes](https://developer.android.com/guide/practices/page-sizes).
