import { useEffect,useState } from 'react';
import { ActivityIndicator,Pressable,SafeAreaView,ScrollView,StyleSheet,Text,View } from 'react-native';
import { router } from 'expo-router';
import { supabase } from '@/lib/supabase';
import { getCurrentContext,AppRole } from '@/lib/session';
import { AppBottomNav } from '@/components/AppBottomNav';
export default function Matches(){const [loading,setLoading]=useState(true),[rows,setRows]=useState<any[]>([]),[role,setRole]=useState<AppRole>('barista');useEffect(()=>{load()},[]);async function load(){const {user,role:r}=await getCurrentContext();if(!user)return router.replace('/login');setRole(r||'barista');let q=supabase.from('applications').select('id,status,barista:profiles!applications_barista_id_fkey(display_name,avatar_url),job:jobs!inner(id,title,owner_id,owner:profiles!jobs_owner_id_fkey(cafe_name,avatar_url))').eq('status','matched');if(r==='barista')q=q.eq('barista_id',user.id);else q=q.eq('job.owner_id',user.id);const {data}=await q;setRows(data||[]);setLoading(false)}if(loading)return <SafeAreaView style={s.safe}><View style={s.center}><ActivityIndicator color="#321708" size="large"/></View></SafeAreaView>;return <SafeAreaView style={s.safe}><View style={s.header}><Text style={s.title}>Matches</Text><Text style={s.sub}>Your hiring connections</Text></View><ScrollView contentContainerStyle={s.list}>{rows.length?rows.map(x=>{const name=role==='barista'?(x.job?.owner?.cafe_name||'Café'):(x.barista?.display_name||'Barista');return <Pressable key={x.id} style={s.row} onPress={()=>router.push({pathname:'/chat/[id]',params:{id:x.id}})}><View style={s.avatar}><Text style={{fontSize:22}}>{role==='barista'?'☕':'👤'}</Text></View><View style={{flex:1}}><Text style={s.name}>{name}</Text><Text style={s.meta}>{x.job?.title||'Matched connection'} · Matched</Text></View><Text style={s.chev}>›</Text></Pressable>}):<View style={s.empty}><Text style={{fontSize:48}}>🤝</Text><Text style={s.emptyTitle}>No matches yet</Text><Text style={s.emptyCopy}>Mutual hiring connections will appear here.</Text></View>}</ScrollView><AppBottomNav active="matches" role={role}/></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#fbf7f1'},center:{flex:1,alignItems:'center',justifyContent:'center'},header:{padding:20,paddingBottom:10},title:{fontSize:31,fontWeight:'900',color:'#321708'},sub:{fontSize:14,color:'#746a61',marginTop:4},list:{padding:18,paddingBottom:30},row:{flexDirection:'row',alignItems:'center',gap:13,backgroundColor:'#fff',borderWidth:1,borderColor:'#eadfd5',borderRadius:18,padding:15,marginBottom:10},avatar:{width:48,height:48,borderRadius:24,backgroundColor:'#f3e8de',alignItems:'center',justifyContent:'center'},name:{fontSize:16,fontWeight:'900',color:'#24150d'},meta:{fontSize:12,color:'#746a61',marginTop:4},chev:{fontSize:29,color:'#a95820'},empty:{alignItems:'center',paddingTop:100},emptyTitle:{fontSize:25,fontWeight:'900',color:'#321708',marginTop:15},emptyCopy:{fontSize:14,color:'#746a61',marginTop:6}});